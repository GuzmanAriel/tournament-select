import {
    Button
  } from 'reactstrap';

const CreateTournamentButton = (props) => { 
  return (
    <Button className="create-tournament primary-cta">Create a Tournament</Button>
  )
    
}

export default CreateTournamentButton;